import { AddAttributeDirective } from './add-attribute.directive';

describe('AddAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
